# B4 Git課題　主な実行コマンド及び確認ポイント


## 1.
### 実行した主なコマンド
- git status（現在の状態を確認）
- git add .（変更をステージ）
- git commit -m "テキストファイルの変更を追加"（変更をコミット）
- git log  -–oneline（現在の状態を確認）

### 確認ポイント
- 初期化（Git管理下にした）の前後で，ディレクトリの中身に何が増えたか
  - .gitという名前のフォルダが増えた．
- ステージ，コミットとは何か
  - ステージは，特定の変更を次のコミットに含めたいということを示すことである．コミットとは，ステージした作業を履歴として実際に保存することを意味する．


## 2.
### 実行した主なコマンド
* git diff（git addしていない変更内容の差分を確認する）
* git add todo.txt（todo.txtの変更をステージ）
* git commit -m "todo.txtから筋トレ削除"（変更をコミット）

### 確認ポイント
* 差分の出力で”+”と”-“はそれぞれ何を表しているか
  * “+”は追加した部分，”-”は削除した部分を表す．
* コミットした後にもう一度差分を表示すると，何が表示されるか
  * git addしたのちコミットを終えているため，git diffでは差分は何も表示されなくなる．


## 3.
### 実行した主なコマンド
* git add todo.txt（todo.txtの変更内容のみをステージ）
* git commit -m "todo.txtから筋トレ削除"（その内容をコミット）

### 確認ポイント
* 「ステージ済みの差分」と「未ステージの差分」を別々に表示するにはどうしたら良いか
  * 「未ステージの差分」は「git diff」を行うことでステージされていない差分のみが表示される．一方，「git diff --staged」を行うと，「ステージ済みの差分」のみを表示することができる．


## 4.
### 実行した主なコマンド
* git restore done.txt（done.txtにある未コミットの変更を取り消す）

### 確認ポイント
* 取り消し前後でファイルの中身がどう変わるか
  * 取り消しを行うと，ステージ及びコミットを行っていない変更点に関しては反映されないようになり，直前にコミットを行った時点での状態に戻る．
* 取り消した変更は，後から元に戻す方法はあるか
  * コミットを行っていない変更に関しては，取り消した後は基本的に復元は不可である．そのため，後から復元する可能性のある変更に関してはコミットやstash等を行っておく必要がある．


## 5.
### 実行した主なコマンド
* git log（今までの全履歴を表示）
* git log –oneline（1行表示で履歴を表示）
* git log -p（各コミットの差分も含め表示）
* git log – todo.txt（todo.txtの変更履歴のみを表示）

### 確認ポイント
* 1つのコミットには，どのような情報が含まれているか
  * コミットに独自に割り振られるID，コミットの作成者，コミットを行った日時，コミット時のメッセージといった情報が含まれている．


## 6.
### 実行した主なコマンド
* type nul > .gitignore（.gitignoreを作成）
* git add .gitignore（.gitignoreをステージ）
* git commit -m "Add .gitignore"（.gitignoreをコミット）

### 確認ポイント
* 設定ファイルを書く前後で，状態表示はどう変わったか
  * 設定ファイルを追加する前は状態表示においてsecret.txtが表示されているのに対し，追加後は状態表示を行ったとしてもsecret.txtは表示されない．
* すでにコミット済みのファイルを，設定ファイルに書くだけで除外できるか
  * 設定ファイルは未追跡ファイルに対して有効であり，すでにコミットされているファイルを設定ファイルに書くだけで除外することはできない．


## 7.
### 実行した主なコマンド
* git remote add origin https://github.com/yosa3834/b4_kadai_yoshida.git（リモートリポジトリを登録）
* git remote -v（正しく登録できたかを確認）

### 確認ポイント
* オプションに，”-u”（”--set-upstream”）を付けると，何が設定されるか
  * ローカルブランチとリモートブランチの対応関係が設定される．


## 8.
### 実行した主なコマンド
* git push（変更をGitHubに反映）

### 確認ポイント
* コミット直後，変更はローカルとリモートのどちらに存在しているか
  * ローカルに存在しており，リモートにはまだ反映されていない．
* pushの前後で，ローカルのブランチとリモート（origin/main）はそれぞれどこを指すようになる
  * push前はローカルブランチは最新のコミットを指しているのに対しリモートブランチはpushを行っていないコミットを除いたコミットのうち最新のものを指す．一方，push後はローカル，リモート共に細心のコミットを指す．


## 9.
### 実行した主なコマンド
* Switched to a new branch 'yoshida_sub_branch'（新たなブランチを作成して切り替える）
* git add .（変更をステージ）
* git commit -m "todo.txtからタスクを1つ削除"（ステージした変更をコミット）
* git commit -m "done.txtにタスクを一つ追加"（ステージした変更をコミット）
* git log --graph --oneline –all（現時点での履歴をグラフ表示で確認）


* （以下「git log --graph --oneline –all」の出力）
```
| * 76e096b (yoshida_sub_branch) todo.txtからタスクを1つ削除
|/
* 1c069bb (origin/master) todo.txtを変更しGitHubに反映
* 8a0dbe0 Add .gitignore
* cdf67f1 todo.txtから筋トレ削除
* a1b2406 todo.txtを編集
* bac71d0 テキストファイルの変更を追加
```

### 確認ポイント
* コミットしたあとにブランチを切り替えると，ファイルの内容はどうなるか
  * 前のブランチでコミットした内容は切り替え先のブランチに反映されておらず，コミット前の状態で表示される．


## 10.
### 実行した主なコマンド
* git switch master（masterブランチに移動）
* git merge yoshida_sub_branch（masterブランチに課題9で作成したブランチをマージ）
* git log --graph --oneline --decorate –all（マージ後の履歴をグラフ表示で確認）
* git branch -d yoshida_sub_branch（課題9で作成したブランチを削除）

### 確認ポイント
* マージ後，mainにはどの変更が取り込まれたか
  * 課題9で作成したブランチにおいてコミットを行った変更が取り込まれた．
* ブランチを分けて作業してからマージするやり方には，どんな利点があるか
  * 複数人が同時に作業を行っても互いの作業を邪魔せずに開発を行うことができる，mainには直接変更を加えないため，不備などがあったとしても簡単にやり直すことができ，完成版に影響しない，などの利点がある．


## 11.
### 実行した主なコマンド
* git fetch（リモートの最新状態を取得するが，ローカルにはまだ反映しない）
* git pull（リモートの最新状態をローカルに取り込む）

### 確認ポイント
* Pull Requestを作成するのは，単にマージするためだけか
  * 変更内容を他の人に確認，評価してもらうなど，他にも利点がある．
* fetchとpullは何が違うのか
  * fetchはリモートリポジトリの状態を更新することのみでき，ローカルのブランチには影響を与えない．一方，pullでは実際にリモートリポジトリの最新の変更を実際にローカルリポジトリにマージすることで取り込むことができる．


## 12.
### 実行した主なコマンド
* git add .（変更をステージ）
* git commit -m "取り消したい変更を追加"（変更をコミット）
* git revert 3a6862f（そのコミットを打ち消すコミットを作成）
* git log –oneline（現在の状態を確認）

### 確認ポイント
* ファイルの中身は元に戻っていても，履歴には何が残っているか
  * 取り消す対象となるコミットと，打ち消すために生成したコミットの両方が残っている．

　
## 13.
### 実行した主なコマンド
* git reset –hard 0ee01f7（0ee01f7のコミットの次以降のコミットを全て履歴ごと消去）

### 確認ポイント
* 課題12の方法と比べて，履歴の見え方はどう違うか
  * 課題12では履歴に取り消す対象となるコミットと打ち消すためのコミットの両方が残っているのに対し，こちらではその両方のコミットが消えている．
* この操作を他人と共有しているブランチで気軽にやってはいけないのはなぜか
  * こちらはローカルだけでなくGitHub上のリモートリポジトリの方のコミットも消去するため，消去するコミットの内容次第では他人の作業にも大きい影響を与える恐れがあるから．
* 変更をなかったことにするために使用したコマンドのオプションによって，何が変わるのか
  * 今回はgit resetにおいて--hardを用いており，作業ディレクトリの変更も含め完全に削除する．一方，コミットのみを取り消す—softや，コミットとステージを戻してaddする前の状態に戻す—mixedなども存在するため，オプションによって戻す範囲が変化すると考えられる．


## 14.
### 実行した主なコマンド
* git stash list（コミットしていない変更を一時退避）
* git stash pop（stashした変更を復元）
* git stash show（stashしている変更を確認）
* git restore done.txt（done.txtにある未コミットの変更を取り消す）

### 確認ポイント
* 退避した変更はgit logには表示されない．どうやって一覧や中身を確認できるか
  * git stashというコマンドを持ちて，退避した変更を確認することができる．
* git stash popとgit stash applyの違いは何か
  * どちらも共に，stashした変更を現在の作業ツリーに復元するためのコマンドであるが，git stash applyはその際にstashの内容を残すのに対し，git stash popは復元が成功するとstashを削除する．


## 15.
### 実行した主なコマンド
* git merge yoshida_sub_branch3（masterブランチに新たに作成したブランチをマージ）
* git merge yoshida_sub_branch4（masterブランチに新たに作成したブランチをマージ）

### 確認ポイント
* Vコンフリクトが起きたとき、ファイルにはどのような印が入るか
  * 以下のように，<<<<<<<，=======，>>>>>>>といったような競合マーカーが入る．<<<<<<<から=======までの部分が現在のブランチ側の変更，=======から>>>>>>>までの部分がマージ対象のブランチ側の変更を表している．
```
	<<<<<<< HEAD
筋トレ（腹筋）
=======
筋トレ（背筋）
>>>>>>> yoshida_sub_branch4
```
* 最終的な内容を決めるときに，どちらかの変更を優先したのか．または両方の変更を反映させたのか（コンフリクトの解決方法は自由）
  * 双方において別々の内容を入力していたため，片方のブランチ（branch3）にもう片方のブランチ（branch4）で追加した文字列を追加することで，両方の変更を反映させた．